var before = ee.Image.constant(2);
var after = ee.Image.constant(4);
print(after);
var sub = before.subtract(after);
//var count = sub.reduceRegion(ee.Reducer.mean())
//count.getInfo()
print(sub);